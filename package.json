{
  "name": "solid-vanilla",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepare": "bun run build",
    "format": "bun x prettier -w *.json ./src",
    "prebuild": "bun run format",
    "build": "tsc"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "~5.9.3"
  }
}
