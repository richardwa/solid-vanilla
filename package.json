{
  "name": "solid-vanilla",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "format": "bun x prettier -w *.json ./src ./test",
    "prebuild": "bun run format",
    "build": "tsc"
  },
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.14.3",
    "prettier": "^3.6.2",
    "typescript": "~5.9.3"
  }
}
