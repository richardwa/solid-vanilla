{
  "name": "solid-vanilla",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "format": "bun x prettier -w *.json ./src",
    "prebuild": "bun run format",
    "build": "tsc"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "~5.9.3"
  }
}
